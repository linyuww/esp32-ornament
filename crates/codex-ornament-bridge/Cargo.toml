[package]
name = "codex-ornament-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
quota-core = { path = "../quota-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
